# PDF 提取长期改进计划

## 背景
当前 PDF 提取使用 `pdf-extract` 库，该库对损坏的 PDF 文件（xref 表损坏但内容完整）的容错能力有限。客户可能上传这类 PDF 文件，导致提取失败。

## 已实现的改进
- ✅ 添加回退机制：当 `pdf-extract` 失败时，使用二进制字符串提取作为备选方案
- ✅ 改进错误处理和日志记录

## 长期改进任务

### 1. 评估替代 PDF 库
**优先级**: 高  
**工作量**: 中等  
**目标**: 找到更宽松的 PDF 库，能处理损坏的 PDF

**候选库**:
- `pdfium-render` - 基于 PDFium（Google Chrome 使用的库）
  - 优点：容错能力强，业界标准
  - 缺点：需要 C++ 依赖
  
- `mupdf` - 业界标准 PDF 库
  - 优点：容错机制完善，广泛使用
  - 缺点：需要 C 依赖
  
- `lopdf` - 纯 Rust 实现
  - 优点：无外部依赖，易于集成
  - 缺点：功能可能不如成熟库完整

**建议**: 先评估 `pdfium-render`，如果 C++ 依赖过重，再考虑 `lopdf`

### 2. 改进 PDF 提取的容错机制
**优先级**: 中  
**工作量**: 小  
**目标**: 增强当前的回退机制

**改进方向**:
- 添加更多回退策略（例如：尝试修复 xref 表）
- 改进二进制字符串提取的准确性
- 添加更详细的日志记录，帮助诊断问题

### 3. 添加 PDF 提取测试覆盖
**优先级**: 中  
**工作量**: 小  
**目标**: 测试各种 PDF 场景

**测试场景**:
- ✅ 标准 PDF（已有）
- ❌ 损坏的 PDF（xref 表错误）
- ❌ 加密的 PDF
- ❌ 扫描的 PDF（图像）
- ❌ 大型 PDF（>10MB）

### 4. 文档更新
**优先级**: 低  
**工作量**: 小  
**目标**: 记录支持的 PDF 格式和限制

**文档内容**:
- 支持的 PDF 格式和版本
- 已知的限制（例如：扫描 PDF 不支持）
- 用户上传 PDF 的最佳实践

## 实现时间表
- **第 1 阶段**（当前）: 添加回退机制 ✅
- **第 2 阶段**（下一个 sprint）: 评估替代库
- **第 3 阶段**（后续）: 实现最佳库并添加测试
- **第 4 阶段**（最后）: 文档更新

## 相关代码
- `src/document_extraction/extractors.rs` - PDF 提取实现
- `tests/fixtures/hello.pdf` - 测试 PDF 文件
- `src/document_extraction/mod.rs` - 文档提取中间件
