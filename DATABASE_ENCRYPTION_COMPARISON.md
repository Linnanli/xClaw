# 数据库加密与秘密加密对比分析

## 概述

IronClaw 采用了**分层加密策略**：
- **秘密级加密**：所有敏感数据（API 密钥、令牌等）使用 AES-256-GCM 加密
- **数据库级加密**：可选，由数据库系统或操作系统提供

本文档对比两种加密方式的优缺点，以及 SQLite 和 libSQL 的适用场景。

## 秘密加密 vs 数据库加密

### 秘密加密（应用层）

**实现方式**：
```
秘密数据
  ↓
AES-256-GCM 加密（应用层）
  ↓
加密后的二进制数据
  ↓
存储在数据库中
```

**优点**：
1. **细粒度控制**
   - 只加密敏感数据（秘密、凭证）
   - 其他数据保持可查询状态
   - 可以对不同秘密使用不同的密钥

2. **性能优势**
   - 只加密必要的数据
   - 数据库查询不需要解密整个表
   - 减少 CPU 开销

3. **灵活性**
   - 支持多种加密算法（AES-256-GCM、SM2/SM3/SM4）
   - 可以在运行时更换加密算法
   - 支持密钥轮换

4. **可审计性**
   - 明确知道哪些数据被加密
   - 可以追踪加密/解密操作
   - 便于安全审计

5. **跨平台一致性**
   - 加密逻辑在应用层
   - 不依赖数据库系统的加密实现
   - 在不同数据库间保持一致

**缺点**：
1. **应用复杂性**
   - 需要在应用层实现加密逻辑
   - 需要管理加密密钥
   - 需要处理加密/解密错误

2. **查询限制**
   - 无法对加密数据进行 SQL 查询
   - 需要先解密再查询
   - 无法使用数据库索引

3. **密钥管理**
   - 需要安全存储主密钥
   - 需要处理密钥轮换
   - 需要防止密钥泄露

### 数据库加密（数据库层）

**实现方式**：
```
秘密数据
  ↓
存储在数据库中
  ↓
数据库系统加密（透明加密）
  ↓
加密后的文件存储在磁盘上
```

**优点**：
1. **透明性**
   - 应用层无需修改
   - 数据库自动加密/解密
   - 对应用透明

2. **全面保护**
   - 所有数据都被加密
   - 包括索引、日志等
   - 防止磁盘被盗

3. **查询能力**
   - 可以对加密数据进行 SQL 查询
   - 数据库索引仍然有效
   - 性能影响较小

4. **简化管理**
   - 密钥由数据库系统管理
   - 无需应用层干预
   - 自动处理加密/解密

**缺点**：
1. **性能开销**
   - 所有数据都需要加密/解密
   - 增加 CPU 使用率
   - 可能影响查询性能

2. **依赖性**
   - 依赖数据库系统的加密实现
   - 不同数据库的加密方式不同
   - 迁移数据库时需要重新加密

3. **密钥管理复杂**
   - 密钥由数据库系统管理
   - 难以实现密钥轮换
   - 难以审计密钥使用

4. **灵活性限制**
   - 无法选择加密算法
   - 无法对不同数据使用不同的加密方式
   - 难以实现细粒度的加密策略

## IronClaw 的加密策略

### 当前实现

**秘密加密**：
```rust
// src/secrets/mod.rs
// 所有秘密使用 AES-256-GCM 加密
// 每个秘密使用 HKDF-SHA256 派生的独立密钥
// 主密钥存储在系统密钥链中
```

**优势**：
1. ✅ 细粒度控制 - 只加密敏感数据
2. ✅ 高性能 - 不加密整个数据库
3. ✅ 灵活 - 支持多种加密算法
4. ✅ 可审计 - 明确的加密边界
5. ✅ 跨平台 - 一致的加密实现

**劣势**：
1. ❌ 无法查询加密数据 - 需要先解密
2. ❌ 应用复杂性 - 需要管理加密逻辑
3. ❌ 密钥管理 - 需要安全存储主密钥

### 建议

**对于 IronClaw 项目**：
- ✅ **保持秘密加密** - 已经实现，性能好，灵活性高
- ⚠️ **可选数据库加密** - 根据部署环境选择
  - 本地部署：可选（文件系统加密已足够）
  - 云部署：推荐（防止云存储被盗）
  - 企业部署：必需（满足合规要求）

## SQLite vs libSQL 对比

### SQLite

**特点**：
```
SQLite
├── 轻量级嵌入式数据库
├── 单文件存储
├── 无服务器架构
├── 广泛使用和成熟
└── 支持 WAL 模式
```

**优点**：
1. **轻量级**
   - 单个文件
   - 无需服务器
   - 易于部署

2. **成熟稳定**
   - 广泛使用
   - 经过充分测试
   - 文档完善

3. **功能完整**
   - 支持 SQL 标准
   - 支持事务
   - 支持触发器

4. **性能**
   - 本地访问快速
   - 支持 WAL 模式
   - 支持并发读取

**缺点**：
1. **并发写入限制**
   - 单个写入锁
   - 并发写入性能差
   - 需要应用层处理

2. **无加密支持**
   - 标准 SQLite 不支持加密
   - 需要第三方扩展（如 SQLCipher）
   - 增加复杂性

3. **无云同步**
   - 无法与云同步
   - 无法实现多副本
   - 难以实现高可用

4. **扩展性限制**
   - 单机限制
   - 无法分布式部署
   - 难以扩展

### libSQL

**特点**：
```
libSQL
├── Turso 开发的 SQLite 分支
├── SQLite 兼容
├── 支持本地和云模式
├── 支持嵌入式副本
└── 现代化设计
```

**优点**：
1. **SQLite 兼容**
   - 100% SQLite 兼容
   - 易于迁移
   - 现有代码无需修改

2. **云同步**
   - 支持 Turso 云同步
   - 嵌入式副本模式
   - 自动同步

3. **改进的并发**
   - 改进的写入性能
   - 更好的并发支持
   - 减少锁竞争

4. **现代特性**
   - 支持向量搜索
   - 支持 JSON 扩展
   - 支持全文搜索

5. **灵活部署**
   - 本地嵌入式
   - 云托管
   - 混合模式

**缺点**：
1. **相对较新**
   - 生态不如 SQLite 成熟
   - 文档相对较少
   - 社区较小

2. **云依赖**
   - 云同步需要 Turso 账户
   - 网络依赖
   - 可能产生费用

3. **学习曲线**
   - 新的概念（嵌入式副本）
   - 需要学习新的配置方式
   - 文档不够完善

4. **加密支持**
   - libSQL 本身不提供加密
   - 需要应用层加密
   - 与 SQLite 相同

## 项目适用性分析

### IronClaw 项目特点

1. **本地优先**
   - 用户数据存储在本地
   - 不依赖云服务
   - 隐私第一

2. **单用户**
   - 通常单个用户使用
   - 并发写入不多
   - 性能要求不高

3. **功能丰富**
   - 需要向量搜索
   - 需要全文搜索
   - 需要 JSON 存储

4. **灵活部署**
   - 支持本地部署
   - 支持 Docker 部署
   - 支持云部署

### 推荐方案

#### 方案 1：SQLite（保守方案）

**适用场景**：
- 本地开发
- 轻量级部署
- 对稳定性要求高

**配置**：
```env
DATABASE_BACKEND=sqlite
DATABASE_URL=sqlite:///path/to/ironclaw.db
```

**优点**：
- ✅ 成熟稳定
- ✅ 无外部依赖
- ✅ 易于部署

**缺点**：
- ❌ 无云同步
- ❌ 并发写入性能差
- ❌ 需要第三方扩展支持加密

#### 方案 2：libSQL 本地（推荐方案）

**适用场景**：
- 本地部署
- 需要向量搜索
- 需要更好的并发性能

**配置**：
```env
DATABASE_BACKEND=libsql
LIBSQL_PATH=/path/to/ironclaw.db
```

**优点**：
- ✅ SQLite 兼容
- ✅ 支持向量搜索
- ✅ 更好的并发性能
- ✅ 现代化设计

**缺点**：
- ❌ 相对较新
- ❌ 生态不如 SQLite 成熟

#### 方案 3：libSQL 云同步（高级方案）

**适用场景**：
- 多设备同步
- 云备份
- 高可用部署

**配置**：
```env
DATABASE_BACKEND=libsql
LIBSQL_PATH=/path/to/ironclaw.db
LIBSQL_URL=libsql://[token]@[org]-[db].turso.io
```

**优点**：
- ✅ 云同步
- ✅ 自动备份
- ✅ 多设备访问

**缺点**：
- ❌ 需要 Turso 账户
- ❌ 网络依赖
- ❌ 可能产生费用

#### 方案 4：PostgreSQL（企业方案）

**适用场景**：
- 企业部署
- 多用户系统
- 高并发场景

**配置**：
```env
DATABASE_BACKEND=postgres
DATABASE_URL=postgres://user:password@localhost/ironclaw
```

**优点**：
- ✅ 企业级功能
- ✅ 高并发支持
- ✅ 完整的加密支持

**缺点**：
- ❌ 需要服务器
- ❌ 配置复杂
- ❌ 资源消耗大

## 加密最佳实践

### 秘密加密

**当前实现**：
```rust
// 所有秘密使用 AES-256-GCM 加密
// 每个秘密使用 HKDF-SHA256 派生的独立密钥
// 主密钥存储在系统密钥链中
```

**最佳实践**：
1. ✅ **保持秘密加密** - 已经实现，性能好
2. ✅ **使用系统密钥链** - 安全存储主密钥
3. ✅ **支持多种算法** - 满足不同地区要求
4. ✅ **定期密钥轮换** - 提高安全性
5. ✅ **审计加密操作** - 记录所有加密/解密

### 数据库加密

**建议**：
1. **本地部署**
   - 使用文件系统加密（LUKS、FileVault）
   - 或使用 SQLCipher（SQLite）
   - 或使用 libSQL 的加密扩展（如果可用）

2. **云部署**
   - 使用云提供商的加密服务
   - 或使用 PostgreSQL 的加密扩展
   - 或使用 Turso 的加密功能

3. **企业部署**
   - 使用 PostgreSQL 的完整加密
   - 或使用 TDE（Transparent Data Encryption）
   - 或使用硬件加密模块（HSM）

## 总结

### 加密策略

| 方面 | 秘密加密 | 数据库加密 |
|------|---------|----------|
| 粒度 | 细粒度 | 全局 |
| 性能 | 高 | 中等 |
| 灵活性 | 高 | 低 |
| 查询能力 | 低 | 高 |
| 复杂性 | 中等 | 低 |
| 推荐 | ✅ 必需 | ⚠️ 可选 |

### 数据库选择

| 方面 | SQLite | libSQL | PostgreSQL |
|------|--------|--------|-----------|
| 轻量级 | ✅ | ✅ | ❌ |
| 成熟度 | ✅✅✅ | ✅✅ | ✅✅✅ |
| 云同步 | ❌ | ✅ | ⚠️ |
| 并发性 | ⚠️ | ✅ | ✅✅ |
| 向量搜索 | ❌ | ✅ | ✅ |
| 推荐 | 本地开发 | 本地部署 | 企业部署 |

### IronClaw 推荐

**当前最佳实践**：
1. ✅ **保持秘密加密** - AES-256-GCM + HKDF
2. ✅ **使用 libSQL 本地** - 本地部署推荐
3. ⚠️ **可选数据库加密** - 根据部署环境选择
4. ✅ **支持 PostgreSQL** - 企业部署选项

**未来改进**：
1. 考虑添加 SQLCipher 支持（SQLite 加密）
2. 考虑添加 libSQL 加密扩展
3. 考虑添加 PostgreSQL 加密支持
4. 实现密钥轮换机制
5. 增强加密审计日志

## 参考资源

- `src/secrets/mod.rs` - 秘密加密实现
- `src/db/libsql/mod.rs` - libSQL 数据库实现
- `src/db/mod.rs` - 数据库抽象层
- `README.md` - 安全架构概述
- [SQLite 官方文档](https://www.sqlite.org/)
- [libSQL 官方文档](https://github.com/tursodatabase/libsql)
- [PostgreSQL 加密文档](https://www.postgresql.org/docs/current/encryption-options.html)
