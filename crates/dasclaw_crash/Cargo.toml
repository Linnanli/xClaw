[package]
name = "dasclaw_crash"
version = "0.0.0-w1"
edition = "2021"
description = "panic_hook + sentry adapter (cross-cutting gap fill)."
license = "Apache-2.0 OR MIT"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "1"
