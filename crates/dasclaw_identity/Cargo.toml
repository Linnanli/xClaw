[package]
name = "dasclaw_identity"
version = "0.0.0-w1"
edition = "2021"
description = "Device-key (P256 ECDSA) + agent-identity (codex port)."
license = "Apache-2.0 OR MIT"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "1"
