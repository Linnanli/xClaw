[package]
name = "dasclaw_observability"
version = "0.0.0-w1"
edition = "2021"
description = "Trait-based observability (Observer / NoopObserver / LogObserver / MultiObserver / PromptCacheMonitor) shared by ironclaw and admin-backend."
license = "Apache-2.0 OR MIT"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
tracing = "0.1"
