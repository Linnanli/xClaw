{
  "enabled": true,
  "name": "代码质量自检",
  "description": "每次写入代码文件后，自动检查是否符合 code-quality.md 规范：禁止补丁式代码、函数不超过 50 行、嵌套不超过 3 层、Rust 代码不用 unwrap() 和不必要的 clone()。",
  "version": "1",
  "when": {
    "type": "promptSubmit"
  },
  "then": {
    "type": "askAgent",
    "prompt": "检查刚刚写入的代码是否符合 code-quality.md 规范。重点关注：\n1. 是否是补丁式代码（在函数末尾追加 if、复制粘贴逻辑、用 flag 控制执行）\n2. 函数是否超过 50 行，嵌套是否超过 3 层\n3. Rust 代码是否使用了 unwrap()（应该用 ? 或 expect）、不必要的 clone()\n4. 是否有相似逻辑出现 3 次以上需要提取\n5. 新增参数是否只服务一个 case\n\n如果发现问题，简要指出并建议修改方向。如果代码质量没问题，不需要输出任何内容。"
  },
  "workspaceFolderName": "x-claw",
  "shortName": "code-quality-check"
}